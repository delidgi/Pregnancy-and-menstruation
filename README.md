# Обновление cycle.4

- Восстановлены ручные роли «Я: альфа/бета/омега» и «Бот: альфа/бета/омега» в омегаверсе. Сохранённые роли используются, если они есть; при отсутствии — я омега, бот альфа. Автоопределения нет.
- Омега получает течку, альфа — гон, бета — ни того ни другого. Активная фаза приходится на дни 12–16 единого 28-дневного счётчика участника. У выбранного вынашивающего сохраняются остальные репродуктивные фазы. Роль не меняет выбор вынашивания.
- У бота и юзера общий шаблон карточек с заголовком, значком, фазой, либидо, настроением и физическим состоянием. Цикл альфы/омеги продвигается и у невынашивающего.
- Контрацепция видна только для отслеживаемого вынашивающего: в сценарии «отслеживать обоих, вынашиваю я» поле контрацепции бота скрыто.
- Удалены выпадающий список и кнопка гигиены из инфоблока. В карточке осталась краткая строка «Гигиена: пора сменить» или время до смены. Управление находится в настройках, кнопка «Сменено» не сжимается в колонку букв.
- 48 автоматических проверок пройдено. Карточки обоих участников и управление гигиеной просмотрены на теме minimal в локальном браузерном предпросмотре. Это не проверка живого SillyTavern с подключённой моделью.

# Обновление cycle.3

- Возвращён отдельный выбор «Отслеживать: меня / бота / обоих». Он не меняет выбор вынашивающего.
- Убрана поясняющая строка с перечислением фаз под вселенной.
- Добавлен переключатель «Месячные включены». Если выключен, дни 1–5 относятся к фолликулярной фазе; овуляция/течка и остальные фазы сохраняются. Кровотечение и гигиенические напоминания выключаются в интерфейсе и промпте.
- В инфоблоке обоих вынашивающих: выбор средства, «Средство сменено», время до смены и напоминание. Отсчёт идёт от отмеченной смены по RP-времени, а не по часам компьютера. Без исходной отметки время не выдумывается. Работает и без дополнительного режима реализма.
- 40 автоматических проверок пройдено; внешний вид карточки с гигиеной проверен в браузере. Живые ответы модели не тестировались.

# Обновление 3.14.0-cycle.1 — 06.09.2026

Основа: Pregnancy-and-menstruation-final-fixed.zip из загрузок (21.08.2026).

## Что изменено
- Один явный выбор: вынашиваю я / вынашивает бот / оба / никто. Он же определяет отслеживание. Пол и роли A/B/O не определяют способность вынашивать.
- У каждого выбранного свой 28-дневный цикл: менструация 1–5, фолликулярная фаза 6–11, овуляция 12–16, лютеиновая 17–28. В режиме «Овуляция / течка» течка — та же фаза 12–16. Отдельные старые счётчики 30/42/70 дней больше не используются.
- Сохранённые дни обычного цикла остаются. Старое «Авто» переносится в явный выбор по прежнему «Отслеживать», без анализа пола. После обновления проверьте выбор вынашивающего.
- Беременность приостанавливает цикл. Исправлены задержки больше 28 дней, переход через конец удлинённого цикла, учёт смены RP-даты и независимый день бота.
- Выключенная скрытая беременность сразу отключает скрытие, в том числе в старых сохранениях с pregnancyKnown=false. Промпт не утверждает, что теста не было или участник ничего не знает. Факты сцены имеют приоритет. Результаты теста и явное раскрытие могут обновлять состояние через RP_STATUS у обоих участников.
- Фаза влияет на контекст обычной сцены даже без гигиенического «реализма». При месячных модель получает указание учитывать наблюдаемые признаки при близости, без телепатии и без решения за игрока.
- В семейном дереве у имён нет подписей «мама/папа». В связанных полях использовано «Второй родитель».

## Установка
Замените файлы существующего расширения содержимым папки Pregnancy-and-menstruation из архива и перезагрузите страницу. Не создавайте вторую копию расширения рядом с первой. Архив не содержит историю Git или пользовательские чаты.

## Проверка
35 автоматических проверок с заглушками окружения SillyTavern: варианты выбора, обе вселенные и границы фаз, независимые циклы, скрытие, тесты/раскрытие, миграция, задержка, смена RP-даты, отсутствие повторного продвижения, генерация интерфейса и дерева. Карточки и настройки дополнительно просмотрены в браузере на локальном предпросмотре.
Это не проверка в запущенной сборке SillyTavern с подключённой моделью. Генерируемые инструкции проверены; реакция конкретной модели зависит также от её пресета и контекста.


---

## Документация предыдущей версии (поведение циклов заменено описанным выше)

# Reproductive System — SillyTavern Extension

Расширение добавляет реалистичную репродуктивную систему в RP: менструальный цикл, зачатие через dice-roll, отслеживание беременности по RP-датам, осложнения, визиты к врачу, роды.

## Установка

https://github.com/delidgi/Pregnancy-and-menstruation

Перезапустите SillyTavern. Расширение появится в панели расширений.

## Как работает

### Менструальный цикл
- 28-дневный цикл с четырьмя фазами: менструация (1-5), фолликулярная (6-11), овуляция (12-16), лютеиновая (17-28)
- День цикла синхронизируется с AI через парсинг текста или авто-продвижение по реальному времени
- Фаза цикла влияет на шанс зачатия: овуляция — x1.65, остальные фазы — x0.25-0.5

### Зачатие (dice-roll)
AI ставит тег `<!-- [CYCLE_DAY:14][CONCEPTION_CHECK] -->` в конце сообщения, когда происходит вагинальная эякуляция внутрь. Расширение:

1. Валидирует контекст (отсекает анальный, оральный секс, будущее время, прерванный акт)
2. Бросает dice: базовый шанс 20% × модификатор фазы цикла
3. Применяет контрацепцию (презерватив 85%, таблетки 91%, ВМС 99%) — с шансом провала
4. При успехе — устанавливает беременность, определяет количество плодов (двойня ~3%, тройня ~0.1%) и пол
5. Инжектирует результат в следующий ответ AI



### Беременность
- Срок считается по RP-датам (парсинг дат из текста AI) относительно даты зачатия
- Настраиваемая длительность: 4-100 недель (по умолчанию 40)
- Промпт-инжекция сообщает AI текущий срок, симптомы, размер плода, ПДР
- UI-панель показывает прогресс-бар, симптомы, рекомендации

### Осложнения
Каждые 7 RP-дней — dice-roll на осложнение. Шанс зависит от триместра и количества плодов:
- 1 триместр (15%): токсикоз, угроза выкидыша, анемия
- 2 триместр (5%): предлежание плаценты, ИЦН, гест. диабет, отслойка
- 3 триместр (12%): гестоз, преждевременные роды, ЗВУР, маловодие

Критические осложнения могут привести к потере беременности (dice-roll на выживание). Кнопка «К врачу» позволяет лечить осложнения (шанс: 75% для warning, 50% для critical), кулдаун 3 RP-дня.

### Роды
AI ставит тег `<!-- [BIRTH] -->` при достижении ≥90% срока. Расширение сбрасывает беременность и показывает уведомление.

## Парсинг RP-дат
Поддерживаемые форматы:
- `Дата: 21 октября 2023` / `Date: 21 October 2023`
- `21.10.2023` / `21/10/23`
- `2023-10-21` (ISO)
- `📅 13/10/23`
- JSON: `date:{"output":"21.10.2023"}`

## Настройки

| Параметр | Описание |
|----------|----------|
| Включено | Вкл/выкл всю систему |
| Уведомления | Показывать нотификации |
| Контрацепция | none / condom (85%) / pill (91%) / iud (99%) |
| День цикла | Ручная установка 1-28 |
| Срок беременности | Длительность в неделях (4-100) |
| Шанс двойни | % (по умолчанию 3) |
| Шанс тройни | % (по умолчанию 0.1) |
| Ручная установка | Установить беременность вручную с указанием срока, количества плодов и RP-даты |

## Архитектура

```
index.js          — точка входа, загрузка, события
config.js         — константы, дефолты, шансы
state.js          — доступ к настройкам и данным беременности (per-chat)
helpers.js        — чистые функции (dice, фазы, симптомы, форматирование)
date-parser.js    — парсинг RP-дат из текста
pregnancy.js      — логика зачатия, осложнений, врача
prompts.js        — инжекция промптов для AI
message-handler.js — обработка входящих сообщений
ui.js             — панель настроек и мониторинг
notifications.js  — кастомные уведомления
```

Данные беременности хранятся per-chat в `extension_settings.reproductive-system.chatPregnancyData[chatId]`.

## Промпт для AI

Расширение инжектирует в чат (depth 0, IN_CHAT):
- Текущий день цикла и фазу
- Инструкцию по тегу `[CONCEPTION_CHECK]` (только если не беременна)
- При беременности: срок, ПДР, симптомы, размер плода, инструкцию по тегу `[BIRTH]`

## Автор

@MommyCat_SillyTavern

## Контекст и производительность
- Один блок контекста вместо двух одинаковых. Убраны повторяющиеся требования, инструкции про внутренние рассуждения и обязательное копирование неизменного PREGNANCY_STATE в каждом ответе. Старые теги по-прежнему распознаются; состояние хранится в расширении.
- Контрольные сценарии, символы во всех активных инъекциях: цикл 8 944 → 1 049; беременность 11 556 → 1 565; один ребёнок 14 754 → 1 394. Это измерение символов, не токенов. Размер меняется с настройками и числом участников/детей.
- Неизменный промпт не записывается повторно. Неизменная карточка не удаляется и не создаётся заново. Убрана лишняя повторная перерисовка; обработка изменений DOM ограничена по частоте и отключена при выключенном инфоблоке.
- История и резервная копия исключаются до глубокого копирования снимка. Повторная финальная обработка одинакового текста пропускается.
- Отладочное оставление тегов отключено. Сохранены только диагностические ошибки: одна запись одного типа не чаще раза в 30 секунд.
- Исправлен путь загрузки CSS: он определяется относительно самого расширения, без жёстко заданной папки Pregnancy и случайного параметра при каждой загрузке.

Обновление cycle.2: восстановлен выбор «Вселенная: Обычная / Омегаверс» и явная передача выбранной вселенной в промпт. Возвращены цвета фаз в настройках обоих участников. 36 проверок пройдено.
